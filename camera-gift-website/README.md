# Camera Gift Website

A heartfelt single-page website with a handwritten letter on paper, floating hearts, and lyric snippets on a purple background.

## Open locally

```bash
open index.html
```

Or serve with any static server:

```bash
python3 -m http.server 8080
```

Then visit http://localhost:8080
